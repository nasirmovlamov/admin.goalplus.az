import TicketTypes from "@/components/ticket-types/TicketTypes";
import Layout from "../../components/layout/Layout";
export default function ticketTypePage() {
  return (
    <Layout title="Tickets">
      <TicketTypes />
    </Layout>
  );
}
